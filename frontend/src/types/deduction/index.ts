export type ResolutionType = 'FULL_RESOLUTION' | 'CULPRIT_CORRECT' | 'WRONG_SUSPECT';
export type DeductionResult = { resultId: string; selectedSuspectId: string; isCorrect: boolean; resolutionType: ResolutionType; acquiredCoreClues: number; totalCoreClues: number; endingId: string };
export type Ending = { endingType: 'TRUE' | 'FALSE' | 'WRONG_FALLBACK'; title: string; fixedContent: string; assetUrl: string | null; selectedSuspect: { id: string; code: string; name: string }; actualCulprit: { id: string; code: string; name: string }; fullTimeline: Array<{ occurredAt: string; title: string; description: string }>; motive: string | null; crimeMethod: string | null; evidenceConnections: Array<{ id: string; code: string; title: string; description: string; relatedClues: Array<{ id: string; code: string; title: string }> }>; suspectSecrets: Array<{ suspect: { id: string; name: string }; facts: string[]; lies: Array<{ claim: string; truth: string; reason: string | null }> }>; missedCoreClues: Array<{ id: string; code: string; title: string; description: string }>; dialectExplanations: Array<{ code: string; dialectText: string; standardText: string; meaning: string; usageContext: string | null }>; reportText: string | null; aftermathText: string | null; reportGeneratedAt: string | null };
