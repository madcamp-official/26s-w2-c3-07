export type RegionRef = { id: string; code: string; name: string };
export type EpisodeProgress = { episodeId: string; episodeCode: string; title: string; region: RegionRef; state: 'NOT_STARTED' | 'IN_PROGRESS' | 'COMPLETED'; bestDifficulty: string | null; bestScore: number | null; firstClearedAt: string | null; lastPlayedAt: string | null; unlockedAt: string | null };
export type HistoryItem = { sessionId: string; episode: { id: string; code: string; title: string; region: RegionRef }; difficulty: string; selectedSuspect: { id: string; code: string; name: string }; isCorrect: boolean; resolutionType: 'FULL_RESOLUTION' | 'CULPRIT_CORRECT' | 'WRONG_SUSPECT'; score: number; startedAt: string; completedAt: string };
export type HistoryPage = { items: HistoryItem[]; page: number; pageSize: number; total: number; totalPages: number };
export type ProgressSummary = { playedEpisodeCount: number; completedEpisodeCount: number; solvedEpisodeCount: number; correctCount: number; fullResolutionCount: number; regionProgress: Array<{ region: RegionRef; totalEpisodes: number; playedEpisodes: number; solvedEpisodes: number }>; recentPlays: HistoryItem[]; unlockedDialectCount: number };
export type DialectUnlock = { expression: string; standardMeaning: string; usageContext: string | null; region: RegionRef; episode: { id: string; code: string; title: string } | null; unlockedAt: string };
