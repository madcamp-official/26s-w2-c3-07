'use client';
import Link from 'next/link';
import { useParams } from 'next/navigation';
import { useState } from 'react';
import { AuthGuard } from '@/features/auth/AuthProvider';
import { useApiResource } from '@/features/api/useApiResource';
import { api } from '@/lib/api-client';
import { ErrorState, LoadingState } from '@/components/ui/ApiState';
import { AppHeader } from '@/components/layout/AppHeader';
import type { DeductionResult, Ending } from '@/types/deduction';
import { ApiError } from '@/types/api';
export default function ResultPage(){const{sessionId}=useParams<{sessionId:string}>();const result=useApiResource<DeductionResult>(`/sessions/${sessionId}/result`);const ending=useApiResource<Ending>(`/sessions/${sessionId}/ending`);const[generating,setGenerating]=useState(false);const[error,setError]=useState<ApiError|null>(null);async function report(){setGenerating(true);setError(null);try{await api.post(`/sessions/${sessionId}/ending/report`);await ending.reload()}catch(e){setError(e as ApiError)}finally{setGenerating(false)}}return <AuthGuard><main className="min-h-screen bg-noir-950 px-6 py-12 text-parchment-100"><div className="mx-auto max-w-3xl space-y-6"><AppHeader />{result.loading||ending.loading?<LoadingState label="판정과 엔딩을 불러오는 중..."/>:result.error?<ErrorState error={result.error}/>:ending.error?<ErrorState error={ending.error}/>:result.data&&ending.data&&<><header className="text-center"><p className="text-xs text-evidence-red">{result.data.resolutionType}</p><h1 className="font-display text-5xl">{ending.data.title}</h1><p className="mt-2">{result.data.isCorrect?'정답입니다.':'잘못된 용의자를 지목했습니다.'}</p></header><section className="border-l-4 border-evidence-red bg-[#e9dfc7] p-6 text-noir-900">{ending.data.fixedContent}</section><section className="border p-5"><h2 className="font-display text-2xl">사건의 진상</h2><p className="mt-3">실제 범인: {ending.data.actualCulprit.name}</p><p>동기: {ending.data.motive??'확인되지 않음'}</p><p>범행 방법: {ending.data.crimeMethod??'확인되지 않음'}</p></section><section><h2 className="font-display text-2xl">전체 타임라인</h2>{ending.data.fullTimeline.map((item,i)=><p key={`${item.occurredAt}-${i}`} className="mt-2 border-l-2 border-brass-400 pl-3"><b>{item.occurredAt}</b> {item.description}</p>)}</section><section><h2 className="font-display text-2xl">사투리 해설</h2>{ending.data.dialectExplanations.map(item=><p key={item.code} className="mt-2">{item.dialectText} — {item.meaning}</p>)}</section>{ending.data.reportText?<section className="border border-brass-600/30 p-5"><h2 className="font-display text-2xl">형사 보고서</h2><p className="mt-3 whitespace-pre-wrap">{ending.data.reportText}</p><p className="mt-4 whitespace-pre-wrap opacity-70">{ending.data.aftermathText}</p></section>:<button onClick={report} disabled={generating} className="w-full border border-brass-400 py-3">{generating?'보고서 생성 중...':'형사 보고서 생성'}</button>}{error&&<ErrorState error={error}/>}<Link href="/regions" className="block bg-evidence-red py-4 text-center font-bold">다른 사건 수사하기</Link></>}</div></main></AuthGuard>}
