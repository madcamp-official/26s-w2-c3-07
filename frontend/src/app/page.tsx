'use client';
import Image from 'next/image';
import Link from 'next/link';
import { useRouter } from 'next/navigation';
import { useAuth } from '@/features/auth/AuthProvider';
export default function HomePage(){const auth=useAuth();const router=useRouter();async function logout(){await auth.signOut();router.replace('/login')}return <main className="relative flex min-h-screen flex-col justify-end overflow-hidden bg-noir-950 px-6 pb-36 text-parchment-100"><Image src="/images/ui/home.png" alt="탐정님, 그 뜻이 아니예라" fill priority sizes="100vw" className="pointer-events-none object-cover object-top" /><div className="pointer-events-none absolute inset-0 bg-gradient-to-b from-transparent via-noir-950/40 to-noir-950" /><div className="relative mx-auto w-full max-w-xl space-y-6 text-center"><p className="font-display text-2xl drop-shadow">{auth.profile?`${auth.profile.displayName??'탐정'}님,`:'사투리 속 진실을 찾아라'}</p><nav className="grid gap-3"><Link className="bg-evidence-red py-4 font-bold" href={auth.profile?'/regions':'/login'}>사건 수사 시작</Link><Link className="border border-brass-600/40 bg-noir-950/60 py-4" href={auth.profile?'/profile/history':'/login'}>수사 기록 보기</Link><Link className="border border-brass-600/40 bg-noir-950/60 py-4" href={auth.profile?'/profile/dialects':'/login'}>사투리 기록집</Link></nav>{auth.profile&&<button onClick={logout} className="text-sm opacity-60">로그아웃</button>}</div></main>}
